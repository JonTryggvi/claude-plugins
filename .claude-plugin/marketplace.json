{
  "$schema": "https://json.schemastore.org/claude-code-marketplace.json",
  "name": "avista",
  "owner": {
    "name": "Avista",
    "url": "https://avista.is"
  },
  "plugins": [
    {
      "name": "avista-memory-tools",
      "source": "./avista-memory-tools",
      "description": "Maintenance tools for the agent-instructions, memory, and git-hygiene layers — bootstrap a project CLAUDE.md from its source, audit existing CLAUDE.md files for content that belongs elsewhere, lint the auto-memory store for staleness and drift, check a branch is in sync with origin/main before pushing, and ship updates to Avista skill-bundle plugins through the org marketplace."
    },
    {
      "name": "avista-wp-releases",
      "source": "./avista-wp-releases",
      "description": "Avista's WordPress release pipeline for plugins and themes — onboard a developer's machine for multi-account GitHub (gh CLI + SSH), scaffold GitHub Releases + plugin-update-checker (PUC v5p6) auto-updates, and ship new versions through that pipeline."
    },
    {
      "name": "avista-wp-prod-ops",
      "source": "./avista-wp-prod-ops",
      "description": "Operate safely on Avista WordPress sites whose code lives on production and is reached over SSH — inspect-first triage, mandatory git backup of DB-stored code, and WP Code Box 2 snippet install/update without the admin UI."
    },
    {
      "name": "avista-wpgb-trp-fix",
      "source": "./avista-wpgb-trp-fix",
      "description": "Diagnose and fix the recurring bug where WP Grid Builder facets and pagination return the default language on TranslatePress sites — root cause is WPGB's AJAX response never passing through TRP's dynamic-content translator. Ships a transport-aware filter pair (query-var AJAX + REST insurance) that translates WPGB responses for the referer's language."
    },
    {
      "name": "avista-chess",
      "source": "./avista-chess",
      "description": "Import your chess.com and lichess games, analyze them with Stockfish, and get a local dashboard with plain-language coaching — a blunder trainer, opening deep-dives, a review queue, and AI coaching (live via your own key, or queued for a Claude session). One skill clones the Avista-Chess app, asks for your usernames, installs dependencies, and launches the dashboard."
    },
    {
      "name": "avista-wp-algolia-chunker",
      "source": "./avista-wp-algolia-chunker",
      "description": "Keep WordPress + Algolia records under the 100KB hard limit (and the ~10KB relevance budget) by chunking long text the right way — route big content through the chunked `content` path of wp-search-with-algolia (which splits via Algolia_Utils::explode_content with distinct), keep small fields as shared attributes, and for non-plugin/custom-attribute cases use a standalone word-boundary chunker that emits {id}-{n} distinct records. Covers long bodies, denormalized search blobs, and extracted PDF text."
    },
    {
      "name": "avista-design-systems",
      "source": "./avista-design-systems",
      "description": "Fetch the org's Claude Design design systems — including the view-only ones that DesignSync's list_projects hides — and brand documents with them. Discovery unions writable projects, a verified id registry, pasted share links and an optional browser harvest, confirming every candidate with get_project so read-only systems shared by a teammate are usable by id. Reads each system's own tokens and logo, then renders a client-ready PDF through headless Chrome so the system's web fonts embed properly."
    },
    {
      "name": "avista-repo-audit",
      "source": "./avista-repo-audit",
      "description": "Read-only security audit for untrusted external repositories before anyone in the org clones, opens, installs, or runs them — screens for indirect prompt injection, supply-chain payloads, install-time hooks (npm postinstall / pip setup.py / composer scripts / Claude-plugin hooks & mcpServers), DNS/HTTP payload delivery, base64/hex obfuscation, reverse-shell primitives, credential exfiltration, and persistence. Clones the target into a throwaway quarantine and runs a deterministic nine-phase scanner; never executes, installs, imports, or builds the target, and never obeys instructions found inside its files."
    },
    {
      "name": "avista-wp-performance",
      "source": "./avista-wp-performance",
      "description": "Diagnose and fix performance problems on Avista WordPress + Breakdance sites — a modular, diagnose-first, approval-gated toolkit for the recurring PHP-worker-saturation problem (pages that sometimes take 30–60s, ~30s media uploads). A read-only audit funnel measures TTFB on a cache HIT vs a cache-busted MISS, rules out object-cache/DB/autoload, tests cache warmth/TTL, and analyzes the nginx access log + php_slow.log to pinpoint the bottleneck; then targeted skills act on it — a bot firewall + robots.txt to recover workers from crawler overload, Breakdance Server-Timing profiling to name the hot templates/nodes, guarded block-theme render micro-optimizations (classic themes only), and a slow-media-upload fix (Smush synchronous optimization + image-size sprawl). Every production change is backed up first, deployed only on explicit approval, verified before/after, and shipped with a one-line rollback; cache-TTL and Breakdance-template work are diagnosed precisely and handed off (WPMU DEV Hub / builder UI) rather than forced over SSH."
    }
  ]
}
